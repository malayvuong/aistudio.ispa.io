# Testing Checklist

- [ ] Auth flow works (no default admin fallback).
- [ ] Provider switch changes backend provider (google/openai).
- [ ] Prisma writes history rows for each feature (package, music, album, visuals).
- [ ] Build passes with typecheck+eslint enabled (`npm run lint`, `npm run build`).
